{
  "name": "the-lab",
  "private": true,
  "description": "The Lab Performance & Strength — monorepo root",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test"
  }
}
